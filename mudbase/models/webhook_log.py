# coding: utf-8

"""
    MUDBASESDK

    MUDBASE is a scalable, real-time, and secure Backend-as-a-Service (BaaS) platform  designed for modern applications. Built with custom logic, it offers fine-grained  control, extensibility, and enterprise-grade security.  ## Features - 🔐 Multi-provider authentication (30+ OAuth providers) - 📊 Real-time database with collections - 📁 File storage and management - 🔑 API key management with permissions - 🔗 Webhook system with retry logic - ⚡ Serverless functions - 💬 Multi-channel messaging (Push, Email, SMS) - 📈 Usage analytics and monitoring - 🌐 Real-time WebSocket events - 🔍 Full-text search capabilities - 💳 Billing: fiat only for project subscriptions and org BaaS checkout (platform fee split). On-chain billing is not exposed on these APIs (optional `crypto-payment-module/` in repo, not mounted by default). - 🏢 Enterprise / Phase 4: custom domains on Growth, Scale, and Enterprise (TXT DNS at `_mudbase-verify.<hostname>`); `settings.customDomainAddon` is optional (billing/legacy); dedicated DB migration script, periodic DNS recheck job, optional `infrastructureEnvironments[]` and edge/metering fields on `dedicated`. 

    The version of the OpenAPI document: 1.3.15
    Contact: support@mudbase.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mudbase.models.webhook_log_response import WebhookLogResponse
from typing import Optional, Set
from typing_extensions import Self

class WebhookLog(BaseModel):
    """
    One **outbound delivery attempt** (Mudbase HTTP client → your `url`). **`_id`** is what the API calls **`webhookId`** in **`POST /api/webhooks/trigger`** and **`POST /api/webhooks/retry/{webhookId}`**. The string field **`webhookId`** below is an internal correlation id (e.g. `manual-<timestamp>`), not the path parameter for retry. 
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="MongoDB id — use as `webhookId` path param for retry", alias="_id")
    org: Optional[StrictStr] = Field(default=None, description="Organization that owns the project")
    project: Optional[StrictStr] = Field(default=None, description="Project id this delivery belongs to")
    webhook_id: Optional[StrictStr] = Field(default=None, description="Internal correlation string (e.g. manual-173…), not the retry path id", alias="webhookId")
    url: Optional[StrictStr] = None
    method: Optional[StrictStr] = None
    event: Optional[StrictStr] = None
    status: Optional[StrictStr] = None
    payload: Optional[Dict[str, Any]] = Field(default=None, description="JSON body sent to your endpoint")
    headers: Optional[Dict[str, Any]] = Field(default=None, description="Outbound request headers (e.g. X-MUDBASE-Event, Content-Type)")
    response: Optional[WebhookLogResponse] = None
    duration: Optional[StrictInt] = Field(default=None, description="Round-trip time in milliseconds")
    attempts: Optional[StrictInt] = None
    max_attempts: Optional[StrictInt] = Field(default=None, alias="maxAttempts")
    error: Optional[StrictStr] = None
    next_retry: Optional[datetime] = Field(default=None, alias="nextRetry")
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    __properties: ClassVar[List[str]] = ["_id", "org", "project", "webhookId", "url", "method", "event", "status", "payload", "headers", "response", "duration", "attempts", "maxAttempts", "error", "nextRetry", "createdAt", "updatedAt"]

    @field_validator('method')
    def method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['GET', 'POST', 'PUT', 'PATCH', 'DELETE']):
            raise ValueError("must be one of enum values ('GET', 'POST', 'PUT', 'PATCH', 'DELETE')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'success', 'failed', 'retrying']):
            raise ValueError("must be one of enum values ('pending', 'success', 'failed', 'retrying')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WebhookLog from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of response
        if self.response:
            _dict['response'] = self.response.to_dict()
        # set to None if error (nullable) is None
        # and model_fields_set contains the field
        if self.error is None and "error" in self.model_fields_set:
            _dict['error'] = None

        # set to None if next_retry (nullable) is None
        # and model_fields_set contains the field
        if self.next_retry is None and "next_retry" in self.model_fields_set:
            _dict['nextRetry'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WebhookLog from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "_id": obj.get("_id"),
            "org": obj.get("org"),
            "project": obj.get("project"),
            "webhookId": obj.get("webhookId"),
            "url": obj.get("url"),
            "method": obj.get("method"),
            "event": obj.get("event"),
            "status": obj.get("status"),
            "payload": obj.get("payload"),
            "headers": obj.get("headers"),
            "response": WebhookLogResponse.from_dict(obj["response"]) if obj.get("response") is not None else None,
            "duration": obj.get("duration"),
            "attempts": obj.get("attempts"),
            "maxAttempts": obj.get("maxAttempts"),
            "error": obj.get("error"),
            "nextRetry": obj.get("nextRetry"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


