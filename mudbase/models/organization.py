# coding: utf-8

"""
    MUDBASESDK

    MUDBASE is a scalable, real-time, and secure Backend-as-a-Service (BaaS) platform  designed for modern applications. Built with custom logic, it offers fine-grained  control, extensibility, and enterprise-grade security.  ## Features - 🔐 Multi-provider authentication (30+ OAuth providers) - 📊 Real-time database with collections - 📁 File storage and management - 🔑 API key management with permissions - 🔗 Webhook system with retry logic - ⚡ Serverless functions - 💬 Multi-channel messaging (Push, Email, SMS) - 📈 Usage analytics and monitoring - 🌐 Real-time WebSocket events - 🔍 Full-text search capabilities - 💳 Billing: fiat only for project subscriptions and org BaaS checkout (platform fee split). On-chain billing is not exposed on these APIs (optional `crypto-payment-module/` in repo, not mounted by default). - 🏢 Enterprise / Phase 4: custom domains on Growth, Scale, and Enterprise (TXT DNS at `_mudbase-verify.<hostname>`); `settings.customDomainAddon` is optional (billing/legacy); dedicated DB migration script, periodic DNS recheck job, optional `infrastructureEnvironments[]` and edge/metering fields on `dedicated`. 

    The version of the OpenAPI document: 1.3.16
    Contact: support@mudbase.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mudbase.models.billing import Billing
from mudbase.models.limits import Limits
from mudbase.models.plan import Plan
from mudbase.models.usage import Usage
from typing import Optional, Set
from typing_extensions import Self

class Organization(BaseModel):
    """
    Organization
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, alias="_id")
    name: Optional[StrictStr] = None
    slug: Optional[StrictStr] = None
    description: Optional[StrictStr] = None
    logo: Optional[StrictStr] = Field(default=None, description="Optional logo URL. Org-related emails use the platform logo (env); this field is for legacy or future UI use only.")
    website: Optional[StrictStr] = None
    plan: Optional[Plan] = None
    usage: Optional[Usage] = None
    limits: Optional[Limits] = None
    billing: Optional[Billing] = None
    settings: Optional[Dict[str, Any]] = Field(default=None, description="May include customDomainAddon (optional billing/legacy flag; not required for custom domains on Growth/Scale).")
    deployment_type: Optional[StrictStr] = Field(default=None, alias="deploymentType")
    dedicated: Optional[Dict[str, Any]] = Field(default=None, description="Dedicated API/DB routing; may include edgeTlsStatus, infraMeteringLastReportAt.")
    preferred_region: Optional[StrictStr] = Field(default=None, alias="preferredRegion")
    infrastructure_environments: Optional[List[Dict[str, Any]]] = Field(default=None, alias="infrastructureEnvironments")
    allowed_domains: Optional[List[Dict[str, Any]]] = Field(default=None, alias="allowedDomains")
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    __properties: ClassVar[List[str]] = ["_id", "name", "slug", "description", "logo", "website", "plan", "usage", "limits", "billing", "settings", "deploymentType", "dedicated", "preferredRegion", "infrastructureEnvironments", "allowedDomains", "createdAt", "updatedAt"]

    @field_validator('deployment_type')
    def deployment_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['shared', 'dedicated']):
            raise ValueError("must be one of enum values ('shared', 'dedicated')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Organization from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of plan
        if self.plan:
            _dict['plan'] = self.plan.to_dict()
        # override the default output from pydantic by calling `to_dict()` of usage
        if self.usage:
            _dict['usage'] = self.usage.to_dict()
        # override the default output from pydantic by calling `to_dict()` of limits
        if self.limits:
            _dict['limits'] = self.limits.to_dict()
        # override the default output from pydantic by calling `to_dict()` of billing
        if self.billing:
            _dict['billing'] = self.billing.to_dict()
        # set to None if preferred_region (nullable) is None
        # and model_fields_set contains the field
        if self.preferred_region is None and "preferred_region" in self.model_fields_set:
            _dict['preferredRegion'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Organization from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "_id": obj.get("_id"),
            "name": obj.get("name"),
            "slug": obj.get("slug"),
            "description": obj.get("description"),
            "logo": obj.get("logo"),
            "website": obj.get("website"),
            "plan": Plan.from_dict(obj["plan"]) if obj.get("plan") is not None else None,
            "usage": Usage.from_dict(obj["usage"]) if obj.get("usage") is not None else None,
            "limits": Limits.from_dict(obj["limits"]) if obj.get("limits") is not None else None,
            "billing": Billing.from_dict(obj["billing"]) if obj.get("billing") is not None else None,
            "settings": obj.get("settings"),
            "deploymentType": obj.get("deploymentType"),
            "dedicated": obj.get("dedicated"),
            "preferredRegion": obj.get("preferredRegion"),
            "infrastructureEnvironments": obj.get("infrastructureEnvironments"),
            "allowedDomains": obj.get("allowedDomains"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


