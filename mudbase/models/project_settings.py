# coding: utf-8

"""
    MUDBASESDK

    MUDBASE is a scalable, real-time, and secure Backend-as-a-Service (BaaS) platform  designed for modern applications. Built with custom logic, it offers fine-grained  control, extensibility, and enterprise-grade security.  ## Features - 🔐 Multi-provider authentication (30+ OAuth providers) - 📊 Real-time database with collections - 📁 File storage and management - 🔑 API key management with permissions - 🔗 Webhook system with retry logic - ⚡ Serverless functions - 💬 Multi-channel messaging (Push, Email, SMS) - 📈 Usage analytics and monitoring - 🌐 Real-time WebSocket events - 🔍 Full-text search capabilities - 💳 Billing: fiat only for project subscriptions and org BaaS checkout (platform fee split). On-chain billing is not exposed on these APIs (optional `crypto-payment-module/` in repo, not mounted by default). - 🏢 Enterprise / Phase 4: custom domains on Growth, Scale, and Enterprise (TXT DNS at `_mudbase-verify.<hostname>`); `settings.customDomainAddon` is optional (billing/legacy); dedicated DB migration script, periodic DNS recheck job, optional `infrastructureEnvironments[]` and edge/metering fields on `dedicated`. 

    The version of the OpenAPI document: 1.3.13
    Contact: support@mudbase.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProjectSettings(BaseModel):
    """
    Project-level settings. Toggles for verification and default user status apply to project-based and role-based signup. 
    """ # noqa: E501
    allow_anonymous_auth: Optional[StrictBool] = Field(default=True, description="Allow anonymous (unauthenticated) users", alias="allowAnonymousAuth")
    require_email_verification: Optional[StrictBool] = Field(default=True, description="When true, users who sign up with email do not receive a token until they verify their email; login is blocked until verified.", alias="requireEmailVerification")
    require_phone_verification: Optional[StrictBool] = Field(default=False, description="When true, users who sign in with phone (e.g. OTP) must have verified their phone before receiving a token.", alias="requirePhoneVerification")
    default_user_account_status: Optional[StrictStr] = Field(default='active', description="Default account status for new signups. **active** = user can use the app immediately. **pending** = user must be approved by an org owner/admin (PATCH org user status to active) before they can perform protected operations. ", alias="defaultUserAccountStatus")
    enable_realtime: Optional[StrictBool] = Field(default=True, alias="enableRealtime")
    enable_storage: Optional[StrictBool] = Field(default=True, alias="enableStorage")
    enable_functions: Optional[StrictBool] = Field(default=False, alias="enableFunctions")
    __properties: ClassVar[List[str]] = ["allowAnonymousAuth", "requireEmailVerification", "requirePhoneVerification", "defaultUserAccountStatus", "enableRealtime", "enableStorage", "enableFunctions"]

    @field_validator('default_user_account_status')
    def default_user_account_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'active']):
            raise ValueError("must be one of enum values ('pending', 'active')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "allowAnonymousAuth": obj.get("allowAnonymousAuth") if obj.get("allowAnonymousAuth") is not None else True,
            "requireEmailVerification": obj.get("requireEmailVerification") if obj.get("requireEmailVerification") is not None else True,
            "requirePhoneVerification": obj.get("requirePhoneVerification") if obj.get("requirePhoneVerification") is not None else False,
            "defaultUserAccountStatus": obj.get("defaultUserAccountStatus") if obj.get("defaultUserAccountStatus") is not None else 'active',
            "enableRealtime": obj.get("enableRealtime") if obj.get("enableRealtime") is not None else True,
            "enableStorage": obj.get("enableStorage") if obj.get("enableStorage") is not None else True,
            "enableFunctions": obj.get("enableFunctions") if obj.get("enableFunctions") is not None else False
        })
        return _obj


