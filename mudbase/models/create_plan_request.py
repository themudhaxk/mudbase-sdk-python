# coding: utf-8

"""
    MUDBASESDK

    MUDBASE is a scalable, real-time, and secure Backend-as-a-Service (BaaS) platform  designed for modern applications. Built with custom logic, it offers fine-grained  control, extensibility, and enterprise-grade security.  ## Features - 🔐 Multi-provider authentication (30+ OAuth providers) - 📊 Real-time database with collections - 📁 File storage and management - 🔑 API key management with permissions - 🔗 Webhook system with retry logic - ⚡ Serverless functions - 💬 Multi-channel messaging (Push, Email, SMS) - 📈 Usage analytics and monitoring - 🌐 Real-time WebSocket events - 🔍 Full-text search capabilities - 💳 Billing: fiat only for project subscriptions and org BaaS checkout (platform fee split). On-chain billing is not exposed on these APIs (optional `crypto-payment-module/` in repo, not mounted by default). - 🏢 Enterprise / Phase 4: custom domains on Growth, Scale, and Enterprise (TXT DNS at `_mudbase-verify.<hostname>`); `settings.customDomainAddon` is optional (billing/legacy); dedicated DB migration script, periodic DNS recheck job, optional `infrastructureEnvironments[]` and edge/metering fields on `dedicated`. 

    The version of the OpenAPI document: 1.3.14
    Contact: support@mudbase.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from mudbase.models.create_plan_request_features_inner import CreatePlanRequestFeaturesInner
from mudbase.models.create_plan_request_limits import CreatePlanRequestLimits
from mudbase.models.create_plan_request_trial import CreatePlanRequestTrial
from typing import Optional, Set
from typing_extensions import Self

class CreatePlanRequest(BaseModel):
    """
    OpenAPI-style body normalized server-side into Plan.pricing (monthly/yearly amounts), features (objects with name/included), and optional limits/trial/metadata. Slug is derived from projectId + name unless a collision occurs. 
    """ # noqa: E501
    name: StrictStr = Field(description="Display name; also used to generate a unique slug per project.")
    description: Optional[StrictStr] = None
    price: Union[StrictFloat, StrictInt] = Field(description="Amount for the chosen interval. The server fills the other billing period (e.g. yearly ≈ monthly × 12 × 0.8 when interval is month). ")
    currency: StrictStr = Field(description="ISO currency code (stored lowercased).")
    interval: StrictStr = Field(description="Which period `price` applies to; drives pricing.monthly vs pricing.yearly.")
    features: Optional[List[CreatePlanRequestFeaturesInner]] = Field(default=None, description="Strings become `{ name, included: true }`. You may send full feature objects instead. ")
    limits: Optional[CreatePlanRequestLimits] = None
    trial: Optional[CreatePlanRequestTrial] = None
    is_active: Optional[StrictBool] = Field(default=True, alias="isActive")
    is_default: Optional[StrictBool] = Field(default=False, description="Only one default plan per project is allowed server-side.", alias="isDefault")
    sort_order: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Lower numbers list first in UIs.", alias="sortOrder")
    metadata: Optional[Dict[str, Any]] = Field(default=None, description="Arbitrary key/value data stored on the plan document.")
    __properties: ClassVar[List[str]] = ["name", "description", "price", "currency", "interval", "features", "limits", "trial", "isActive", "isDefault", "sortOrder", "metadata"]

    @field_validator('interval')
    def interval_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['month', 'year']):
            raise ValueError("must be one of enum values ('month', 'year')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreatePlanRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in features (list)
        _items = []
        if self.features:
            for _item_features in self.features:
                if _item_features:
                    _items.append(_item_features.to_dict())
            _dict['features'] = _items
        # override the default output from pydantic by calling `to_dict()` of limits
        if self.limits:
            _dict['limits'] = self.limits.to_dict()
        # override the default output from pydantic by calling `to_dict()` of trial
        if self.trial:
            _dict['trial'] = self.trial.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreatePlanRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "description": obj.get("description"),
            "price": obj.get("price"),
            "currency": obj.get("currency"),
            "interval": obj.get("interval"),
            "features": [CreatePlanRequestFeaturesInner.from_dict(_item) for _item in obj["features"]] if obj.get("features") is not None else None,
            "limits": CreatePlanRequestLimits.from_dict(obj["limits"]) if obj.get("limits") is not None else None,
            "trial": CreatePlanRequestTrial.from_dict(obj["trial"]) if obj.get("trial") is not None else None,
            "isActive": obj.get("isActive") if obj.get("isActive") is not None else True,
            "isDefault": obj.get("isDefault") if obj.get("isDefault") is not None else False,
            "sortOrder": obj.get("sortOrder"),
            "metadata": obj.get("metadata")
        })
        return _obj


